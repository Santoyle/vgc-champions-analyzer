# Instalar con: pip install -r requirements-dev.txt
# Requiere requirements.txt instalado primero.
-r requirements.txt

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
pytest-xdist>=3.0,<4.0

# Detección de drift — excluido de producción porque
# numba/llvmlite no tiene wheel para Python 3.14 en Streamlit Cloud.
# Se necesita a partir del Bloque 9.
alibi-detect==0.12.0

# Linting / formateo
ruff>=0.4,<1.0
black>=24.0,<25.0
mypy>=1.10,<2.0
pre-commit>=3.7,<4.0
pandera[mypy]>=0.20,<1.0
